# See the note in the repository root .npmrc.
#
# npm reads the project config relative to the working directory, so the
# dashboard needs its own copy — a root file does not apply when `npm install`
# runs from here. Without it, NODE_ENV=production in the ambient shell means
# Tailwind, TypeScript and the ESLint config never install.
include=dev
